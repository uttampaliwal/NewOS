[package]
name = "turnix-std"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
turnix-abi = { path = "../../shared/abi" }
libturnix = { path = "../libturnix" }
