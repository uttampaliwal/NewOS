[package]
name = "init"
version = "0.1.0"
edition = "2021"

[dependencies]
newos-abi = { path = "../../shared/abi" }

[profile.release]
panic = "abort"
lto = true
codegen-units = 1

[profile.dev]
panic = "abort"
