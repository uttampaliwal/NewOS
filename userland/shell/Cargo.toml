[package]
name = "shell"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "shell"
test = false
required-features = ["host_bin"]

[dependencies]
turnix-abi = { path = "../../shared/abi" }
libturnix = { path = "../libturnix" }

[features]
host_bin = []

