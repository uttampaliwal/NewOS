[package]
name = "desktop-shell"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "desktop-shell"
test = false
required-features = ["host_bin"]

[lib]
name = "desktop_shell"
path = "src/lib.rs"

[dependencies]
turnix-abi = { path = "../../shared/abi" }
libturnix = { path = "../libturnix" }

[features]
host_bin = []
