[workspace]
members = [
    "boot/uefi-loader",
    "shared/abi",
    "tools/xtask",
    "kernel",
]
default-members = [
    "shared/abi",
    "tools/xtask",
]
resolver = "2"

[workspace.package]
edition = "2024"
license = "MIT OR Apache-2.0"
authors = ["Uttam", "Codex"]
repository = "https://example.com/newos"
