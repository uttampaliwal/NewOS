[package]
name = "turnix-serial"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
# No external dependencies needed for the basic serial driver
